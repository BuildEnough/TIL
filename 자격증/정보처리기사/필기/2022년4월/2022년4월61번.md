## 문제
C언어에서 문자열 처리 함수의 서식과 그 기능의 연결로 틀린 것은?
1. strlen(s) - s의 길이를 구한다
2. strcpy(s1, s2) - s2를 s1으로 복사한다
3. strcmp(s1, s2) - s1과 s2를 연결한다(X)
4. strrev(s)-s를 거꾸로 변환한다.

## 풀이
strcat
- 문자열 연결 함수

strcmp
- 문자열 비교